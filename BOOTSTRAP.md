# Documentation about set-up of project

## Backend

**Setup**:
```bash
mkdir backend && cd backend
cdk init app --language typescript
```

Deploy:
```bash
npm run cdk deploy
```

## CICD
```bash
mkdir pipeline && cd pipeline
cdk init app --language typescript
```
