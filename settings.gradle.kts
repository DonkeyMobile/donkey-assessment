include(":app", ":presentation", ":domain", ":data")
rootProject.name = "Donkey Mobile"
pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}